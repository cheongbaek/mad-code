# 자작 자율주행 전기차 제어 코드 정리

## 1. 차량 하드웨어 개요

- **제어보드** : Arduino Mega 2560 (모든 코드 공통)
- **배터리** : 58V (구동용). 아두이노/로직 측 12V는 별도 SMPS를 통해 공급
- **주행(인휠모터)**
  - 인휠모터 2개, 양 뒷바퀴에 1개씩 장착
  - 모터컨트롤러(KLS7275H) 2개, 모터 1개당 컨트롤러 1개
  - PWM 배선은 좌/우로 **분리 완료** : 왼쪽 = 8번, 오른쪽 = 9번 핀 (0717 이후. 그 이전에는 10번 하나를 양쪽 컨트롤러가 공유했음)
  - 모터 : QSWP72V5000W(QS260 계열), 16극쌍(=32극) — 허브모터 직결 구동(감속기 없음), 홀센서 위상각 120°, 무부하 최대 1320RPM (제조사 판매처 스펙 확인, 2026-07-13)
  - 타이어 : 175/60R13
  - 각 모터컨트롤러에서 나오는 홀센서 3개 신호를 SN74HC86N(XOR 게이트)으로 합산 → 컨트롤러당 합산 신호 1개
    - **바퀴 1회전당 96펄스**(3상 홀 6엣지 × 16극쌍, `CHANGE` 인터럽트 기준). 1펄스/20ms창 = 0.884 m/s = 3.18 km/h. 상세 근거와 정정 경위는 10절 말미 참고
  - **확정 배선 (2026-07-30, `iw_0730_check.ino`로 후보 6핀 전수 계측하여 검증)**
    - 왼쪽 : 합산신호 → **2번 핀**(인터럽트) → PID → **8번 핀** PWM
    - 오른쪽 : 합산신호 → **21번 핀**(인터럽트) → PID → **9번 핀** PWM
    - 좌/우 각각 '자기 바퀴의 펄스'로 '자기 바퀴의 PWM'을 닫는다(교차 없음) = **모터별 독립 PID 완료**
    - ※ 홀센서 핀은 버전마다 다르다 : `kasa_0717_A`/`kasa_0727_A`는 21(왼쪽)/20(오른쪽), `kasa_0727_A_nodiff`는 2/3을 썼다. 구형 파일을 참고할 때 주의
  - **기동 초반 펄스 노이즈(실측 확인)** : "코일에 힘은 들어갔으나 바퀴가 아직 안 도는" 구간에서 홀신호에 수백~1500 단위 허수 펄스가 12~15주기(240~300ms) 동안 쏟아지고, 바퀴가 구르기 시작하면 사라진다. 물리적 상한은 20ms 창당 약 **42.2펄스**(1320RPM = 22회전/초 × 96펄스/회전 × 0.020초)이므로 그 이상은 전부 허수. (2026-08-04 PPR 정정 반영 — 이전에는 64펄스 기준 28.2로 적혀 있었다. 실제 코드의 허수 판정값 `PULSE_SANITY_MAX`=40은 42.2 바로 아래라 여전히 타당하다.) 대응은 11절 참고
- **조향**
  - DC모터 + 그와 맞물린 가변저항(포텐셔미터)으로 위치 제어
  - 모터드라이버 : **MD20A** (VNH2SP30 기반 2채널 DC모터 드라이버)
    - 채널당 연속 20A(순간 30A), 동작전압 5~30V, PWM+DIR 방식 제어
    - 본 프로젝트에서는 조향용으로 1채널만 사용 : DIR = 22번 핀, PWM = 6번 핀
    - DIR 핀 레벨로 회전방향, PWM 듀티로 회전속도 결정
  - `dc_0701_potential.ino`로 좌/우 끝값을 먼저 측정 → 안전 구동범위 산출
  - `dc_0701_dirtest.ino`로 DIR 핀 레벨과 실제 회전방향(좌/우)이 소프트웨어 정의와 일치하는지 확인
- **제동(브레이크)**
  - 인휠모터와 별개인 리니어모터가 브레이크 페달을 직접 밟는 구조
  - 페달에도 가변저항이 있어 얼마나 밟혔는지 감지 (별도)
  - **현재 리니어모터는 장비 미준비 상태 → 코드에 자리만 확보**
- **E-stop**
  - 40번 핀에 연결, 별도 장치가 스위치를 제어
  - **40번 핀이 단락(LOW)되면 e-stop 발동**으로 판단
  - 리니어모터와 마찬가지로 관련 하드웨어는 아직 미준비 → 코드에 자리만 확보

제어 주기: `kasa_0708_together.ino`는 인휠/조향/브레이크 제어 루프가 **20ms 공통 주기(`CONTROL_WINDOW_MS`)를 공유**한다 (실차 인휠 튜닝이 20ms 기준으로 검증됨. 텔레메트리 출력만 50ms). `iw_0708_pidtuning.ino`와 `dc_0702_pd.ino`도 20ms로 통일 (조향 PD게인을 kasa와 그대로 호환하기 위함). 게인·FF는 창 크기에 종속되므로 주기를 바꾸면 재튜닝이 필요하다. 구형 테스트 코드(`iw_0707_*`)만 50ms 기준으로 남아 있다. (`dc_0701_potential.ino`는 제어가 아닌 단순 모니터링 코드이므로 규칙 제외)

**버전 관리 방침** : 테스트 코드는 파일명의 날짜 숫자(`0707`, `0708`, ...)를 올려가며 새 파일로 만든다. 이전 날짜 파일은 그 시점의 코드를 보존하는 스냅샷으로 남긴다.

## 2. 공통 코딩 규칙 (`dc_0701_potential.ino` 제외)

- **`delay()`/`micros()` 지양, `millis()` 지향** : 정확한 시간 계산을 위해 모든 시간 지연/주기 판단은 `millis()` 기반 상태머신으로 구현한다. (`dc_0701_potential.ino`는 단순 모니터링 코드라 예외)
- **홀센서 인터럽트는 `CHANGE` 모드로 수신** : 펄스 수를 2배로 증폭하기 위함 (`RISING`/`FALLING` 단독 사용 금지)
- **MCU는 항상 Arduino Mega 2560 사용**
- **12V 전원은 배터리(58V)와 별도의 SMPS를 통해 공급**

## 3. 파일별 설명

| 파일 | 목적 |
|---|---|
| `dc_0701_potential.ino` | 조향 DC모터를 손으로 돌려 A1 포텐셔미터의 좌/우 끝값을 측정 → 이후 안전 구동범위(raw값) 산출용 |
| `dc_0701_dirtest.ino` | DC 조향모터의 실제 설치 방향이 올바른지 확인 (왼쪽 0.5초 → 오른쪽 0.5초 살짝 회전 후 종료) |
| `dc_0702_pd.ino` | DC 조향모터 PD 위치제어 테스트. 시리얼로 목표각도(-40~40)를 입력받아 이동, 도달 후 대기 |
| `iw_0707_pwm2pulse.ino` | 인휠모터 PID 제어에 앞서, PWM 값과 그에 따른 2번/3번 핀 펄스값을 각각 확인하기 위한 테스트 코드 |
| `iw_0707_pidtuning.ino` | (구버전 스냅샷) 인휠 PID 튜닝 코드. 속도 피드백은 3번 핀 펄스만 사용, 50ms 주기. 왼쪽 모터 이상폭주 문제가 있었음 |
| `iw_0708_pidtuning.ino` | (완료/스냅샷) 인휠 PID 튜닝 코드. 왼쪽 모터(2번 핀) 피드백 + FF + anti-windup으로 튜닝 완료 (kp=20/ki=2/kd=2). 결과는 통합코드에 반영됨 |
| `kasa_0708_together.ino` | 주행/조향/브레이크를 하나의 시리얼 프로토콜로 통합 제어하는 코드. 인휠 PID는 0708 튜닝 완료본 반영 (리니어/E-stop 하드웨어 미장착 상태이므로 해당 부분은 자리만) |
| `kasa_0712_none.ino` | (스냅샷) A/B 보드 분리 이전 마지막 통합 버전. 인휠 PID(FF+PID+코스트캐치) + 조향/브레이크 + E-stop(13번 핀)을 한 보드에서 처리. 이후 A/B 분리 아키텍처로 대체됨 (10절 참고) |
| `iw_0712_pid.ino` | 인휠 PID 단독 튜닝용 코드 (`kasa_0712_none.ino`의 인휠 로직만 분리). FF 테이블 실측 재보정 및 오버슈트 억제 튜닝이 이 파일에서 진행됨. 현재 확정 게인/구조는 10절 참고 |
| `iw_0709_pwm2pulse.ino` | (실측 도구) PWM 60~170을 10단위로 5초씩 스윕하며 각 단계 정착 후 펄스수를 측정하는 캘리브레이션 코드. `iw_0712_pid.ino`의 FF 테이블 재보정에 쓰인 실측 데이터의 출처 |
| `kasa_0713_A.ino` | **A보드**(인휠 PID + 주행펄스) — 2보드 분리 아키텍처. 상세는 10절 참고 |
| `kasa_0713_B.ino` | **B보드**(조향 + 제동) — 2보드 분리 아키텍처. 상세는 10절 참고 |
| `kasa_0713_protocol.md` | A/B보드 입출력 시리얼 프로토콜 및 E-stop 배선/판정 조건 정리 문서 |
| `estop_0713.ino` | E-stop 배선(13번 핀, NC) 단락/개방 여부만 0.5초마다 시리얼 출력하는 배선 확인용 단순 테스트 코드. A/B 보드 구분 없이 사용 가능 |
| `iw_0716_pid.ino` | (스냅샷) 인휠 **좌우 독립 PID** 최초 도입판. 21번→8번(왼쪽), 20번→9번(오른쪽)으로 각자 루프를 닫고 게인도 좌우 분리. 폭주 감지 추가 |
| `iw_0730_check.ino` | (실측 도구) PID/FF/안전장치를 전부 뺀 순수 배선 검증용. 받은 PWM을 좌우에 그대로 출력하고 후보 홀센서 핀 6개(2,3,18,19,20,21)를 전부 인터럽트로 걸어 어느 핀에 펄스가 들어오는지 확인한다. **현재 확정 배선 2/21의 출처** |
| `iw_0730_pid.ino` | 인휠 PID 단독 튜닝용 (`kasa_0730_A.ino`의 인휠 로직만 분리). 기동 블랭킹·중앙값 필터를 이 파일에서 먼저 검증한 뒤 A보드로 이식했다. 원시/필터 펄스를 함께 출력해 노이즈 진단이 가능 |
| `kasa_0730_A.ino` | (스냅샷) **A보드** 인휠 좌우 독립 PID + 쓰로틀 페달 계측. 상세는 11절 참고 |
| `kasa_0731_A.ino` | (스냅샷) A보드 — `[0731-1]` E-stop **즉시 발동**(500ms 디바운스 제거 + PCINT 인터럽트로 개방 즉시 PWM 차단). 제어 로직은 0730과 동일 |
| `kasa_0731_B.ino` | (스냅샷) B보드 — `[0731-1]` 리미트 스위치(D2/NC) 추가, `[0731-3]` 조향 하드리밋 실측 확정(좌 576 / 우 362), `[0731-4]` E-stop 즉시 발동 |
| **`kasa_0804_A.ino`** | **현행 A보드** — `[0804-1]` E-stop **발동에도 500ms 확인**(발동/해제 대칭, PCINT 제거). 상세는 12절 참고 |
| **`kasa_0804_B.ino`** | **현행 B보드** — `[0804-1]` 브레이크를 엔코더 실측 시간표 방식으로 전환(단계 0/1/2), `[0804-2]` 리미트 스위치 → 리니어 엔코더(D2/D3), `[0804-3]` E-stop = 리니어 2단 체결/해제 시 0단 자동복귀, `[0804-5]` E-stop 발동에도 500ms 확인. 상세는 12절 참고 |
| `kasa_0730_B.ino` | **B보드**(조향 + 제동 + 주행모드 스위치). 상세는 11절 참고 |
| `linear_0803.ino` | (계측 도구) 로터리 엔코더 **E40S8-2048-3-T-5** 단독 동작확인. 모터·드라이버를 전혀 건드리지 않는 순수 계측 코드로, A=D2/B=D3(C상 미사용, 5V·GND)을 양쪽 CHANGE 인터럽트로 받아 x4 디코딩(1회전 8192카운트)한다. 손으로 축을 돌려 ①카운트 변화 ②방향 부호 ③한 바퀴 ±8192 ④전이오류(err) 0을 확인한다. `r` 입력으로 리셋. 리니어(브레이크)에 위치 피드백을 붙이기 전 배선 검증용 |
| `linear_0803_pot.ino` | (계측 도구) 리니어 **행정 최대·최소 실측** — 조향의 `dc_0701_potential.ino`와 같은 역할의 리니어판. 엔코더(D2/D3)만 쓰고 모터는 구동하지 않는다. **부팅 순간의 위치가 항상 영점(0)**, 이후 영점 대비 변화량 정수 하나만 출력하며, 출력 주기는 **5ms 고정**(값이 안 변해도 매 주기 = 등간격 표본. PD는 20ms로 돌 예정이라 4개씩 골라내면 그 주기가 된다). 시리얼로 `0` 한 줄을 넣으면 그 지점이 새 영점(리셋 버튼과 동일). **인터럽트는 `attachInterrupt` 대신 INT4/INT5 벡터 직결**(1회 5~6µs → 2~3µs, 누락 한계가 약 1,200RPM → 2,500RPM으로 두 배)이며, 그래서 핀이 D2/D3에 고정이다. 엣지 유실 시에만 `# ERR n` 줄이 추가로 나온다 — ★같은 위치에서 값이 틀어질 때 이 줄이 없으면 전기적 누락이 아니라 기계적 유격(1카운트=0.044°, 80카운트=3.5°)이라는 판별 근거★. 유실이 감지되면 직전 진행방향으로 ±2카운트를 이어 붙여 보정한다(`RECOVER_LOST_EDGES`) — 버리면 빠른 쪽 방향만 덜 세어져 기준점이 한쪽으로 밀린다.<br>**실측(2026-08-04, 페달 5회)** : `0→380→4→399→−63→309→−106→233→−187→158→−242`. 행정 크기는 380/395/372/339/345로 일정한데 뗀 위치만 평균 −55/사이클로 단조 이동 = 기계적 미끄러짐의 전형. 전체 행정 400카운트=17.6°, 드리프트 55카운트=2.4°(행정의 14%). ★결론 : 증분 엔코더는 절대위치를 모르므로 PD 버전에는 **복귀 시 재영점 수단(리미트 스위치 또는 페달 가변저항)이 필수**★ — 이 파일로 재는 것은 '행정 크기'까지다.<br>사용법 : 한쪽 끝에서 켠 뒤 반대쪽 끝까지 움직여 마지막 숫자를 읽으면 그것이 행정 전체 카운트 = 이후 PD 위치제어의 상한이 된다. 숫자 하나만 나가므로 시리얼 플로터로도 볼 수 있다 |
| `ksae_canlogging.ino` | (참고/이전 버전) EZkontrol CAN(METER V1.1, 0x180117EF/0x180217EF) 전체 필드 수신 + SD카드에 CSV 직접 기록 + 값 변화 시 시리얼 이벤트 로그·에러 비트 해석까지 포함한 풀스펙 로거. `y_can.ino`가 이 파일의 SD 로깅 역할을 대체함 (13절 참고) |
| **`y_can.ino`** / **`y_can.py`** | **현행 CAN 텔레메트리 브리지**. SD카드 없이 필요 필드(속도/전압/전류/기어)만 시리얼로 전송 → PC(`y_can.py`)가 받아 CSV 기록. 상세는 13절 참고 |

## 4. `iw_0707_pwm2pulse.ino` 출력 형식

시리얼로 PWM(0~255)을 입력받아 그대로 출력하고, 50ms 주기로 아래 형식을 출력한다.

```
(pwm) (2번펄스) (3번펄스) (합산펄스)
```

- `2번펄스`, `3번펄스` : 각 주기(50ms) 동안 해당 핀에서 카운트된 펄스 수
- `합산펄스` : `2번펄스 + 3번펄스`

이 코드로 좌/우 모터컨트롤러 합산신호가 정상적으로 들어오는지, PWM 대비 펄스 응답이 어떤지를 먼저 확인한다.

## 5. `dc_0701_dirtest.ino` 방향 확인

- 소프트웨어가 정의한 "왼쪽(DIR_CW)"/"오른쪽(DIR_CCW)" 방향과 실제 모터 설치 방향이 일치하는지 확인하는 코드
- `delay()` 없이 `millis()` 기반 상태머신(`T_LEFT → T_GAP → T_RIGHT → T_DONE`)으로 동작: 왼쪽 0.5초 회전 → 관성 정지 대기 0.3초 → 오른쪽 0.5초 회전 → 종료(`T_DONE` 상태에서 정지 유지)
- PWM 값(`TEST_PWM`)은 실측 후 100으로 조정됨 (최초에는 `kasa_0708_together.ino`의 `STEER_MIN_PWM`을 참고값으로 사용했으나, 실제로 반응하는 수준에 맞춰 값을 올림)
- 실제로 왼쪽 → 오른쪽 순으로 돌지 않으면 `DC_DIR_PIN` 배선 또는 `DIR_CW`/`DIR_CCW` 정의(및 `kasa_example_control.py`의 `invert` 플래그)를 재확인해야 함

## 6. `dc_0702_pd.ino` PD 위치제어

- 조향 DC모터에 PD 위치제어를 접목하는 테스트 코드. 핀 구성과 제어 구조는 `kasa_0708_together.ino`의 조향부를 참고했으며, 게인(`KP_S`, `KD_S`)과 상한/하한 PWM(`STEER_MIN_PWM`, `STEER_MAX_PWM`), 안전 관련 값들을 모두 파일 상단에 두어 조절 가능하게 함
- **입력** : 시리얼로 목표각도(정수, `-40 ~ 40`) 한 줄 입력
  - `-40`/`+40`은 실측 좌/우 하드 리밋(raw, `RAW_LEFT_LIMIT`/`RAW_RIGHT_LIMIT`, `dc_0701_potential.ino`로 측정한 값으로 교체 필요)보다 `SAFETY_MARGIN`(5)만큼 안쪽으로 매핑됨
  - 범위 밖 값/숫자가 아닌 입력은 무시(별도 e-stop 없음, 조향 단독 테스트 코드)
- **하드 리밋 페일세이프** : 현재 raw값이 실측 하드 리밋(`RAW_LEFT_LIMIT`/`RAW_RIGHT_LIMIT`)에 도달하면 PD 계산과 무관하게 즉시 PWM 0
- **도달 판정** : `|오차| <= STEER_TOLERANCE`(3)가 `SETTLE_MS`(0.5초) 이상 연속으로 유지되면 도달로 판정 → 모터 정지 후 대기 상태(`ST_SETTLED`)로 전환
- **대기 상태** : 대기 상태에서는 가변저항 값이 바뀌어도 무시하고 정지를 유지하며, 새로운 각도값이 시리얼로 입력될 때만 제어를 재개함(`ST_ACTIVE`로 전환)

## 7. `iw_0707_pidtuning.ino` 속도 피드백 (구버전 스냅샷)

- 속도 피드백으로 3번 핀 펄스만 사용 (2번 핀 인터럽트 미연결), 제어주기 50ms, 게인 kp=2.0 / ki=0.2 / kd=1.2
- anti-windup은 "출력 포화 시 적분 누적 중단 + pwm 2회 계산" 방식, 미분은 오차 기반(`err - lastErr`)
- **실차 테스트에서 왼쪽 모터 이상폭주 발생** : PWM은 양쪽 컨트롤러에 공통 분배되는데 피드백은 오른쪽(3번 핀)만 보므로, 왼쪽 모터는 사실상 개루프 상태였던 것이 원인으로 추정됨 → `iw_0708_pidtuning.ino`에서 개선

## 8. `iw_0708_pidtuning.ino` 속도 피드백 (튜닝 완료 / 스냅샷)

0707 버전의 왼쪽 모터 이상폭주를 억제하고 튜닝을 완료한 버전. **결과는 `kasa_0708_together.ino`의 인휠 PID에 반영 완료.** 0707 대비 변경점:

- **피드백 소스 변경 (핵심)** : 2번(왼쪽, `encCountL`)/3번(오른쪽, `encCountR`) 핀을 각각 카운트하고, **PID 제어는 왼쪽(2번 핀) 펄스를 사용**. 3번은 모니터링 전용. 폭주하던 왼쪽 모터 자신의 펄스로 루프를 닫은 것이 폭주 억제의 주요 원인
- **제어주기 20ms**. 주의: 20ms 창의 펄스 수는 50ms 창의 40%이므로 여기서 잡은 게인을 50ms 코드에 그대로 이식하면 안 됨
- **미분항** : 측정값 기준(`-(speed - lastSpeed)`, derivative on measurement) — 목표값 계단 변경 시 미분 킥 없음
- **줄 단위 시리얼 파싱** : `parseInt()`의 CRLF/타임아웃 문제(target이 제멋대로 0으로 리셋)를 줄 버퍼 + 숫자 검증 방식으로 해결

### 실차 튜닝 경과 요약

1. 순수 P(kp=2)로 시작 → 출력이 데드존(~60 PWM) 아래에 갇혀 저속 목표에서 기동 불가 확인
2. **피드포워드 도입** : 실측 정적 맵(데드존 ≈60 PWM, 25펄스 ≈ 220 PWM) 기반 `ff = 60 + 6.4×target`. PID는 잔차 보정만 담당
3. **anti-windup 개선** : "출력이 포화된 방향으로는 적분 중단" (하강 스텝에서 −I_MAX까지 쌓인 적분 빚이 언더슛·재가속(엑셀 밟기) 리밋사이클을 만들던 문제 해결) + `±I_MAX`(100) 클램프 병행
4. **목표값 변경 시 적분 리셋**
5. 최종 게인 **kp=20 / ki=2 / kd=2** 에서 만족스러운 응답 확인

### 남은 개선 아이디어 (미적용)

- 목표 20 부근 상승 스텝에서 약간의 오버슛 잔존 (원인: FF가 커서 P의 포화 탈출이 목표 직전(3.3펄스 전)이고, 모터 상한(25)까지 달릴 여지가 남는 구간 + FF 선형 근사가 20에서 ~+20 PWM 과대) → 개선안: 목표값 램프(slew-rate limiter), kp 축소(8~10), FF 구간별 테이블화
- **좌/우 모터 개별 PID (차후 계획)** : 좌/우 모터의 PWM 특성이 일부 달라, 현재 공통으로 물려 있는 PWM 배선을 분리하고 모터별로 독립 PID를 돌려야 할 것으로 예상. 배선 분리 작업 이후 진행

## 9. `kasa_0708_together.ino` 통합 프로토콜

### 입력 (PC → 아두이노)

한 줄에 정수 3개를 공백으로 구분하여 전송 (개행으로 종료):

```
<주행목표값> <조향각도> <브레이크강도>
```

예) `32 -20 5`

- **주행목표값** : 인휠모터 목표 속도(펄스/주기). 0 이상 정수로 처리(음수는 0으로 clamp)
- **조향각도** : 도(degree) 단위, `-30 ~ +30` 범위로 clamp 후 `dc_0701_potential.ino`로 측정한 좌/우 끝값(raw)에 매핑되어 조향 목표 위치로 사용됨
  - 매핑 상수(`STEER_POT_LEFT`, `STEER_POT_RIGHT`, `STEER_ANGLE_MAX`)는 `kasa_example_control.py`의 기존 보정값(L=944, R=645, angle=±30)을 기본값으로 넣어두었으며, `dc_0701_potential.ino`로 재측정 시 코드 상단에서 갱신 필요
- **브레이크강도** : `0 ~ 255` 범위로 clamp. 리니어모터 미장착 상태라 실제 출력은 없고 값만 기록됨(`brake_output`)

### 인휠 PID (0708 튜닝 완료본 반영)

- 왼쪽 모터(2번 핀) 펄스로 루프를 닫음. 3번 핀(오른쪽)은 모니터링 전용
- 피드포워드 `ff = FF_DEADZONE(60) + FF_GAIN(6.4) × wheelREF` + PID 보정
- 포화 방향 적분 중단(anti-windup) + `±I_MAX`(100) 클램프, 측정값 기준 미분, 목표값 변경 시 적분 리셋
- 게인 kp=20 / ki=2 / kd=2, **인휠/조향/브레이크 제어 루프 모두 20ms 공통 주기(`CONTROL_WINDOW_MS`) 공유** (튜닝 검증 주기. 텔레메트리 출력만 50ms)
- E-stop 발동 중에는 적분·펄스 카운터를 초기화해 해제 후 잔재 없이 재개

### 출력 (아두이노 → PC)

50ms 주기로 아래 형식을 출력:

```
(2번펄스) (3번펄스)
```

- `2번펄스` : 왼쪽 모터 펄스 (20ms 창 기준, PID 피드백에 사용되는 값)
- `3번펄스` : 오른쪽 모터 펄스 (20ms 창 기준, 모니터링용)

### E-stop 조건 (아래 중 하나라도 충족 시 발동)

1. 40번 핀 단락 (LOW)
2. 입력 형식 오류 (정수 3개 공백 구분 형식이 아닌 경우 — 개수 불일치, 숫자가 아닌 토큰 포함 등)
3. 입력이 5초 이상 들어오지 않음

### E-stop 동작

- 조향(DC)모터 PWM = 0
- 인휠모터 PWM = 0
- 리니어(브레이크)모터 최고출력으로 구동 (미장착 상태이므로 실제 `analogWrite`는 주석 처리, 로직과 자리만 준비됨)
- 출력 펄스값을 `999 999`로 강제 표시 (실제 펄스 대신 `pulse2_out`, `pulse3_out` 변수에 999를 대입)

## 10. 2보드 분리 아키텍처 (`kasa_0713_A/B.ino`, 2026-07-13~)

### 배경

`kasa_0712_none.ino`까지는 인휠 PID/주행펄스 + 조향/제동을 아두이노 1대에서 처리했으나, 이후 **인휠모터 PID + 주행펄스(A보드)** / **조향 + 제동(B보드)** 두 대로 분리하기로 함. 두 보드는 서로 통신하지 않고 각자 독립적으로 동작한다.

### 핀 배정

- **A보드** (`kasa_0713_A.ino`) : 10(인휠 PWM), 20/21(홀센서), 13(E-stop)
- **B보드** (`kasa_0713_B.ino`) : 6/7(DC 조향 DIR/PWM), 8/9(리니어 브레이크 DIR/PWM), 13(E-stop)

### E-stop (양 보드 공통, 독립 판정)

- 스위치 AK-C211008의 NC 접점 2개를 **직렬** 연결: `아두이노GND ― COM2/NC2 ― COM1/NC1 ― 13번`
- 이 13번 라인을 **A보드·B보드 양쪽에 병렬로 연결**, 각 보드가 INPUT_PULLUP으로 독립 감지(500ms 연속 개방 시 확정 디바운스) + 자기 시리얼 라인 기준 5초 무입력 타임아웃
- 두 보드는 서로 통신 없이 각자 감지 시 자기 PWM만 정지하고 `STOP` 출력 → **한쪽 보드만 감지해도 그 보드는 반드시 정지**하는 페일세이프 구조
- A·B 보드는 GND가 공통이어야 함(별도 전원이면 GND끼리 연결 필요)
- ROS2(`walker_k` 노드)는 두 시리얼 포트 중 **한쪽에서라도** `STOP` 수신 시 전체 정지로 처리(OR)

### 시리얼 프로토콜 (상세는 `kasa_0713_protocol.md`)

- A·B는 PC(ROS2)와 **각각 별도의 USB/시리얼 포트**로 연결 (115200 baud)
- A입력: 주행목표펄스 정수 1개. A출력: `S,<21번펄스>,<20번펄스>` (50ms 주기)
- B입력: `<조향각>,<브레이크>` 콤마 2개. B출력: `P,<조향각환산값>` (가변저항 미장착 상태라 현재 항상 `P,0` 고정, 추후 ADC 장착 시 교체)
- 양쪽 모두 E-stop 중엔 `STOP` 출력 (접두어 없음)

### 인휠 PID 튜닝 경과 (`iw_0712_pid.ino`)

1. **FF 테이블 실측 재보정** : `iw_0709_pwm2pulse.ino`로 PWM 60~170 스윕 실측 → 기존 테이블(펄스 13 이상 구간)이 실측과 크게 어긋나 있어 실측치로 전면 교체
2. **오버슈트 원인 진단** : 목표펄스 진입 시 pwm이 FF+P만으로 곧바로 최대치 근처까지 치솟아 정지 마찰/제동 수단 없는 인휠모터에 과도한 관성이 붙는 것이 원인으로 확인
3. **대응** : PWM 슬루레이트 제한(사이클당 +4, 하강은 무제한), 조건부 적분 누적(|오차| < 4일 때만 적분), kp를 1.0 → 0.4로 완화, 최종 적분 기여를 ki 값과 무관하게 ±40 pwm로 클램프
4. **확정 게인** : `kp=0.4 / ki=0.03(±40 클램프) / kd=0.2`, `PWM_SLEW_MAX=4`, `I_ACCUM_ERR_MAX=4` — target 5, 15 양쪽에서 오버슈트 없이 수렴 확인(2026-07-13 실차 테스트)

### 참고: 목표펄스 → 차량속도 환산

> ⚠️ **[2026-08-04 정정] PPR은 64가 아니라 96이다.** 아래는 정정된 계산이다. 이전 판은 "홀신호 2개를 XOR → 전기적 1회전당 4엣지 × 16극쌍 = 64펄스 → 15펄스 ≈ 71.6km/h"로 적혀 있었는데, XOR 대상이 **3신호**이므로 엣지 수가 다르다.

- **홀센서 3개**를 XOR 합산(120° 위상차). 3상 홀 조합은 **전기적 1회전당 6개 상태**를 가지고, 상태가 바뀔 때마다 정확히 한 신호만 반전되므로 XOR 출력도 매 전이마다 토글한다 → **전기적 1회전당 6엣지**. `CHANGE` 인터럽트가 상승·하강을 모두 세므로 6카운트.
- 6엣지 × 16극쌍 = **바퀴 1회전당 96펄스**(PPR), 직결 구동(감속기 없음) 기준
- 목표펄스 15(/20ms 창) → 750펄스/초 → **469RPM** → 175/60R13 타이어(외경 540.2mm, 둘레 1.6971m) 기준 **약 47.7km/h**
- 1펄스(20ms 창) = **0.884 m/s = 3.18 km/h**
- ★ 96이 맞다는 근거 : **실측과 일치한다** ★ `kasa_ws/src/nxde/nxde/master.py`의 실차 실측 상수 `PULSE_TO_KMH = 3.18`(및 PULSE_SPEED.md의 15펄스 = 47.7km/h)과 소수 둘째자리까지 같다. 64로 계산하면 4.77km/h가 되어 실측의 정확히 **1.5배**(=96/64)만큼 어긋난다 — 이 1.5배 불일치가 오래 남아 있던 "이론값 71.6 vs 실측 47.7" 의 원인이었다.
- 이 환산의 코드상 단일 소유자는 `white_ws/src/white/white/kasa_units.py`다(ROS2 측). 아두이노 코드는 펄스 단위로만 동작하므로 이 상수를 쓰지 않는다.

## 11. 0730 버전 (`kasa_0730_A/B.ino`, 2026-07-30~)

10절의 2보드 구조를 그대로 유지하되, 핀 배선이 확정되고 좌우 독립 PID로 전환되었으며 기동 노이즈 대책이 추가되었다.

### 핀 배정 (확정, `iw_0730_check.ino`로 검증)

- **A보드** : 홀센서 2(왼쪽)/21(오른쪽), 인휠 PWM 8(왼쪽)/9(오른쪽), 쓰로틀 페달 A1, E-stop 13
- **B보드** : DC 조향 6/7, 리니어 브레이크 8/9, 주행모드 스위치 D5, E-stop 13

### 시리얼 프로토콜 (0713 대비 변경점)

- **A입력** : `<값>` 단일(펄스 전용 0~15, 좌우 동일) 또는 `<좌값>,<우값>` 콤마 2값. 콤마 형식에서는 값 범위로 모드가 갈린다 — **0~15 = 펄스 목표(PID) / 16~255 = 직접 PWM(무보호) / 256 이상 = 정지**. 단일 값으로는 직접 PWM을 줄 수 없다(일반 주행 경로에서의 오발동 방지)
- **A출력** : `S,<왼쪽펄스>,<오른쪽펄스>,<쓰로틀raw>` (50ms). 쓰로틀은 A1 가변저항 raw 0~1023이며 A보드는 이 값으로 아무 제어도 하지 않는다(계측·보고 전용). **펄스 필드는 필터 후 값**(PID가 실제로 본 값)이다
- **B출력** : `P,<조향각>,<모드>` — 모드는 D5 스위치(통전=자율주행 1 / 개방=수동조종 0)
- **B입력** : 조향각 자리에 `x`를 넣으면 **조향 힘빼기**(수동조종 시 사람이 핸들을 잡으므로 DC모터에 힘이 들어가면 안 됨)
- E-stop 중에는 양 보드 모두 `STOP` 출력 (0713과 동일)
- ROS2 측은 kasa_ws(nxde) 대응 완료

### 인휠 기동 노이즈 대책 (A보드 `[0730-2]`, `iw_0730_pid.ino`에서 검증)

1절의 기동 노이즈(허수 펄스 수백~1500)에 대해 **구간별로 역할을 나눈 2단 대응**을 쓴다.

- **기동 블랭킹(LAUNCH) — 개루프 구간** : 정지 상태에서 목표가 0 → 양수로 바뀌면 진입. 이 구간은 피드백을 **아예 보지 않고** FF(target)까지 `PWM_SLEW_MAX`(+4/cycle)로 램프만 한다(PID·코스트캐치·폭주감지 전부 정지). 바퀴가 안 도는 상태에서 할 일은 FF 테이블이 실측으로 갖고 있는 정적 PWM까지 밀어붙이는 것뿐이라 피드백을 버려도 잃는 정보가 없다 → **반응성 손실이 없다**
  - 종료 조건은 **(a) pwm이 FF에 도달(램프 완료) + (b) 정상 펄스 `LAUNCH_SETTLE_CYCLES`(5)주기 연속**, 또는 `LAUNCH_MAX_MS`(3초) 타임아웃
  - ★ (a)가 반드시 필요하다 ★ (b)만 쓰면 램프 초반(pwm 4·8·12)에는 코일 힘이 약해 노이즈가 시작되지도 않아 speed가 `0 0 0 0 0`으로 읽히고, 노이즈가 오기도 전에 블랭킹이 조기 종료된다
  - 종료 시 `pidLastPwm`/`pidLastErr`를 현재값으로 이어받아 PID로 무단절 인계
- **3점 중앙값 필터 — 주행 구간** : 바퀴가 구른 뒤 남는 단발성 스파이크(1~2 사이에 갑자기 17) 제거용. 지연이 1주기(20ms)뿐이라 차량 관성 시정수 대비 무시할 수준(5점은 40ms라 쓰지 않음). 허수 판정(`PULSE_SANITY_MAX`=40 초과)된 값은 버퍼에 **아예 넣지 않아** 노이즈 주기엔 직전 정상값이 자동 대입되는 효과가 난다
- **실패안전** : 허수가 `NOISE_HOLD_MAX_CYCLES`(50주기=1초) 연속되면 홀센서 이상으로 보고 코스트(PWM 0). 블랭킹 중에는 노이즈가 정상이므로 세지 않는다

**시도했다가 폐기한 방식(재발 방지용 기록)** : "허수가 들어온 주기는 PID를 건너뛰고 직전 pwm 유지". 리셋은 막았지만 램프업 **진행**까지 멈춰서, 램프 도중 노이즈가 시작되면 pwm이 그 지점에 얼어붙었다. target 2의 FF는 70이고 모터 기동점은 약 60인데 pwm이 40에서 얼면 바퀴가 영원히 못 구르고 → 노이즈도 안 그치고 → 실패안전 타이머가 코스트로 내림 → 0부터 재램프 → 또 40에서 얼음의 무한루프. 실측 로그(`40 40 2 2 1027 681 …`)에 그대로 찍혔다.

### 알려진 한계

- 중앙값은 스파이크만 잡고 **저속 양자화 잡음은 못 잡는다** — 20ms 창에서 1~2펄스면 ±1펄스가 값의 50~100%라 원리적으로 거칠다. 저속 정밀제어가 필요해지면 계측 창을 늘리는 쪽(예: 60ms 이동합)을 검토해야 하며, 그만큼 지연이 늘어나는 트레이드오프가 있다

---

## 12. 0804 버전 (`kasa_0804_A/B.ino`, 2026-08-04~) — **현행**

11절의 2보드 구조·핀 배정·프로토콜을 그대로 유지한다. 0731을 거쳐 달라진 것은 **브레이크 구동 방식**과 **E-stop 판정**이다.

### E-stop 판정 : 발동·해제 모두 500ms 연속 유지 (A보드 `[0804-1]` / B보드 `[0804-5]`)

경위가 세 번 바뀌었으니 정리해 둔다.

| 버전 | 발동 | 해제 | 수단 |
|---|---|---|---|
| ~0730 | 500ms 연속 개방 | 즉시 | loop 폴링 |
| 0731 | **즉시** | 500ms 연속 단락 | PCINT 인터럽트(개방 즉시 PWM 차단) |
| **0804** | **500ms 연속 개방** | 500ms 연속 단락 | loop 폴링 (**PCINT 제거**) |

- **왜 0731의 즉시 발동을 되돌렸나** : 13번 라인은 NC 2접점 직렬에 A·B 보드가 병렬로 붙고 차량 배선을 길게 타므로 접점 채터링·유도 노이즈에 노출된다. 즉시 발동은 그 노이즈 한 번에 주행이 끊긴다는 뜻이고, 주행 중 오발동 자체가 위험(조향 힘빠짐, 뒤차 추돌)이라 판단해 확인 시간을 되살렸다.
- **PCINT를 함께 제거한 이유** : ISR의 존재 이유가 '개방을 본 즉시 PWM 차단'이었다. 발동에 500ms 확인을 요구하는 순간 그 즉시성은 요구와 모순되고, 남겨두면 500ms 미만 노이즈에서 **PWM만 순간 끊겨** 인휠은 램프업이 리셋되고 조향은 핸들이 덜컥거린다. 판정은 `digitalRead` 폴링만으로 하며 loop가 수 kHz로 돌아 해상도가 남는다.
- 구현은 대칭 2타이머(`estop_high_t` / `estop_low_t`)다. 반대 레벨이 한 번이라도 관측되면 해당 타이머를 0으로 리셋해 '연속' 유지를 강제하고, 상태별로 필요한 타이머만 돌린다(전환 직후 낡은 값으로 즉시 재전환되는 것을 막는다).
- 부팅 시 이미 개방이어도 첫 루프부터 발동 타이머가 돌아 500ms 뒤에 발동한다. 그 구간은 모터 정지·시리얼 명령 없음이라 위험하지 않다.

> ### ⚠️ 트레이드오프 — 반드시 알고 쓸 것
> 비상정지가 **최대 0.5초 늦게** 걸린다. 그 사이 이동거리:
>
> | 속도 | 0.5초 이동 |
> |---|---|
> | 2.65 m/s (9.5 km/h, 3펄스) | 약 **1.3 m** |
> | 4.42 m/s (15.9 km/h, 5펄스) | 약 **2.2 m** |
> | 13.26 m/s (47.7 km/h, 15펄스) | 약 **6.6 m** |
>
> 즉 이 설정은 **저속 시험 주행 전제**다. 속도를 올릴 때는 `ESTOP_TRIGGER_CONFIRM_MS`를 100~200ms로 줄이거나, 노이즈 원인을 배선에서 해결한 뒤 0(=0731의 즉시 발동)으로 되돌려야 한다. 두 보드에서 **같은 값으로** 바꿀 것.
> ★ 이 핀은 어디까지나 '소프트웨어 비상정지'다. 전원을 직접 끊는 물리적 최후 수단은 별개로 있어야 한다. ★

### 브레이크 : 열린루프 고정시간 → 엔코더 실측 시간표 (B보드 `[0804-1]`)

- 입력은 **단계 0 / 1 / 2** (★0~255 PWM이 아니다★). `0` = 기본위치(놓음) / `1` = `POS_FULL`의 1/3(83카운트) / `2` = `POS_FULL`(250카운트). 고치는 곳은 `POS_FULL` 하나이고 1단계는 비율로 따라온다.
- 리니어에 로터리 엔코더(E40S8-2048-3-T-5, D2/D3 INT4/INT5 직결)를 붙였지만 **폐루프는 실패했다**(`linear_0804.ino` 실측 : 목표 150에 217에서 멈춤 = 오버슛 49%. PWM 끊은 뒤에도 관성으로 26~75카운트 더 감). 그래서 `linear_0803_speed.ino`로 "몇 ms 밀면 몇 카운트"를 실측해 표로 넣고, 필요한 카운트를 시간으로 역보간해 그 시간만큼만 민다. **엔코더는 제어에서 빠지고 보험으로만 남는다**(목표선 도달 / `ENC_HARD_MAX` 330카운트 초과 시 정지).
- 표가 선형이 아니다 — 50~140ms는 약 0.9카운트/ms인데 150ms부터 1.8카운트/ms로 빨라진다(마찰 이탈 가속). 그래서 단일 기울기가 아니라 **구간 선형보간**을 쓴다. 이 구부러짐이 이 방식의 핵심 근거다.
- `0`단(기본위치) 복귀만은 표를 쓰지 않고 하드스톱까지 500ms 밀어 **재영점**한다(증분 엔코더의 누적 드리프트 대책). 대기 중 3초간 조용하면 그 지점을 새 기준점으로 잡는 자동 재영점도 있다.
- E-stop = 브레이크 2단과 같은 작용이며, 체결 시간만 표가 아니라 `ESTOP_PUSH_MS`(250ms, 최대 행정)를 쓴다(부족한 것이 위험하므로). **해제 시 송신측을 기다리지 않고 스스로 0단으로 복귀한다.**

### ROS2 측 영향

- 프로토콜은 **그대로**다(송신측 수정 불필요). B보드 입력의 브레이크 필드 의미만 `0/1/2` 단계로 확정되었다.
- 발동이 500ms 늦어지므로 `white_ws`의 `/estop` 반영도 그만큼 늦다. `nxde/arduino.py`는 보드가 보내는 `STOP` 문자열로 판정하므로 코드 변경은 없다.
- ★ `white_ws/src/nxde/arduino.py` 주석은 아직 A보드를 `kasa_0730_A.ino`로 적고 있다 ★ 프로토콜이 동일해 동작에는 영향이 없지만, 참조 파일명은 `kasa_0804_A.ino`로 읽으면 된다.

---

## 13. EZkontrol CAN 텔레메트리 브리지 (`y_can.ino`/`y_can.py`, 2026-08-05~)

인휠/조향/브레이크와는 별개 축인 **구동계 모니터링**(CAN버스 계측) 라인. A/B보드와 통신하지 않는 독립된 세 번째 아두이노 코드다.

### 배경 : `ksae_canlogging.ino` → `y_can.ino`

- `ksae_canlogging.ino`(참고/이전 버전)는 EZkontrol MCU→METER CAN 메시지 두 종(`0x180117EF`: 전압/전류/속도, `0x180217EF`: 온도/상태/에러)을 전량 수신해 **아두이노가 직접 SD카드**(`canlog.csv`)에 기록하고, 값이 바뀔 때만 시리얼에 이벤트 로그(기어/브레이크/모드/에러 비트 해석)를 남기는 풀스펙 로거였다.
- `y_can.ino`는 한때 SD를 떼고 시리얼만 내보냈으나, **2026-08-06에 SD 기록을 되살려 지금은 두 갈래로 동시에 낸다**(아래 표). CAN 필드 전량은 SD가 받고, 시리얼은 대시보드에 필요한 5개만 보낸다.
- CAN 파라미터는 두 파일이 동일(전량 계승) : MCP2515(HW-184 모듈), CS=53 / INT=2, `CAN_250KBPS` / `MCP_8MHZ`, `MCP_LISTENONLY`(버스에 아무것도 송신하지 않음 — 차량 CAN에 안전하게 얹혀 듣기만 함). METER V1.1 스케일링(전압/전류 ×0.1, 전류 오프셋 −3200, 속도 오프셋 −32000)도 동일

### 데이터 경로 두 갈래 (2026-08-06~)

| | SD카드 (아두이노가 직접) | 시리얼 → PC (`y_can.py`) |
|---|---|---|
| 담는 열 | **16열 전부** | 5개 필드 → CSV에선 6열만 채우고 **10열은 공란** |
| 주기 | 500ms (`SD_INTERVAL_MS`) | 100ms (`SERIAL_INTERVAL_MS`) |
| 속도 정밀도 | 원본 rpm | rpm/10 절사 → PC가 ×10 복원 |
| 파일명 | `CANLOG01.CSV` … `CANLOG99.CSV` (**부팅마다 새 파일**) | `canlog_001.csv` … (기록마다 새 파일) |

- **양식은 완전히 동일한 16열**(`CANLOG.CSV`와 헤더 한 글자까지 같다). 그래서 SD에서 뽑은 CSV와 PC가 쓴 CSV를 `y_can.py`가 **같은 코드로 읽고 같은 그래프를 그린다**. 실측 검증: 저장소의 `CANLOG.CSV`(실제 SD 출력물)를 그대로 불러 4개 창이 정상 생성됨
- SD 주기를 시리얼과 같은 100ms로 올리면 안 된다 — `flush()`가 수~수십ms 블로킹하는 동안 MCP2515 수신버퍼(2개)가 넘쳐 CAN 프레임을 놓친다. 500ms는 이전 로거에서 검증된 값
- **부팅마다 새 파일**인 이유 : 이어붙이면 `millis()`가 0으로 되돌아가 시간축이 뒤로 뛴다. (읽는 쪽에도 방어가 있어 음수 dt 구간은 적분에서 제외하지만, 애초에 안 만드는 게 낫다)
- 파일명은 `snprintf` 대신 자릿수를 직접 박는다(`logName[6]`/`[7]`) — SD는 8.3 이름만 확실히 먹고, 포맷 문자열 코드가 붙는 것도 피한다
- 기어/브레이크/모드/에러가 바뀔 때 시리얼에 남기는 이벤트 줄은 **`#` 접두**라 `y_can.py`의 파싱을 깨지 않는다

### `y_can.ino` 배선/안전 처리 (기존 로거에서 정리한 점)

- **SD_CS(4번 핀)를 안 쓰는데도 부팅 시 HIGH로 고정** : MCP2515와 SD 모듈이 SPI 버스를 공유하므로, SD 모듈이 물려 있는 채로 그 CS가 뜨면 SD가 MISO를 잡아 CAN 수신이 깨질 수 있다. SD를 쓰지 않는 이 코드에서도 배선이 남아 있는 한 반드시 눌러둬야 하는 값
- **MCP2515 초기화 실패 시 무한 `while(1);`로 멈추지 않고 시리얼로 계속 보고** : 1초마다 `# MCP2515 Initialization Failed - check CS/SPI wiring` 출력. `#`로 시작하는 줄은 `y_can.py`가 파싱 없이 건너뛰므로 정상 데이터 줄 형식을 깨지 않고 알릴 수 있다(`linear_0803_pot.ino`의 `# ERR n` 관례와 동일한 패턴)
- **`readMsgBuf()`가 `CAN_OK`를 반환한 프레임만 디코드** : 읽기 실패 시 버퍼에 남는 쓰레기값을 그대로 쓰면 PC측(향후 전력량 적분 등)에 잘못된 값이 누적되므로 실패한 프레임은 통째로 버림

### 시리얼 프로토콜

- **출력 주기 100ms**(`ksae_canlogging.ino`의 500ms보다 빠름 — SD 쓰기 없이 시리얼만 내보내므로 가능)
- 한 줄 형식(헤더 없음, 콤마 5개) : `<속도_rpm/10 절사값>,<배터리전압_V>,<배터리전류_A>,<상전류_A>,<기어(0~7)>`
  - ★ 속도 필드는 원본 rpm을 10으로 나눈 **정수 절사값**이다(예: 6543 → 654) ★ 정밀 RPM이 아니라 "모터가 얼마나 돌아가는지" 대략적인 확인용이라 마지막 한 자릿수를 버려 최대 6000대 값을 3~4자리로 줄임. 실제 rpm이 필요하면 ×10 해서 역산해야 한다
- 이 줄을 `y_can.py`가 파싱해 16열 CSV의 `motorSpeed_rpm` 열에 **×10 복원한 값**으로 기록한다(10의 배수만 나온다). SD쪽에는 원본 rpm이 그대로 남으므로 정보가 사라지지는 않는다

#### CSV 양식 (16열, SD·PC 공통)

```
millis,batteryVoltage_V,batteryCurrent_A,phaseCurrent_A,motorSpeed_rpm,controllerTemp_C,
motorTemp_C,accelPct,gear,brake,opMode,dcContactor,err1_hex,err2_hex,err3_hex,lifeCounter
```

- PC가 채우는 6열 : `millis`(기록 시작 기준 경과 ms) / `batteryVoltage_V` / `batteryCurrent_A` / `phaseCurrent_A` / `motorSpeed_rpm` / `gear`(숫자가 아니라 `NO`/`R`/`D1` 같은 **이름**)
- PC가 공란으로 두는 10열 : `controllerTemp_C` `motorTemp_C` `accelPct` `brake` `opMode` `dcContactor` `err1_hex` `err2_hex` `err3_hex` `lifeCounter` — 시리얼로 나오지 않는 값들이다. 실측 예 : `0,66.0,40.0,88.0,1000,,,,D1,,,,,,,`
- ★ **배터리% / 전력량 같은 파생값은 CSV에 넣지 않는다** ★ 양식을 SD 출력과 똑같이 유지해야 하므로, 읽을 때 `compute_derived()`로 그 자리에서 다시 계산한다. 실측 검증: 실시간 누적값과 파일 재계산값이 소수 5자리까지 일치

### `y_can.py` — tkinter GUI 대시보드 (2026-08-06 전면 재작성)

콘솔 스크립트에서 **GUI 앱**으로 바뀌었다. 실행하면 먼저 "이전 데이터 **CSV 파일** 선택" 창이 뜨고(취소하면 빈 상태로 시작), 이후 대시보드가 뜬다. ★폴더가 아니라 파일을 고른다 — SD카드에서 뽑은 `CANLOG01.CSV`를 바로 지정할 수 있다★

```
실시간 대시보드
[모터속도 rpm] [배터리 전압 V] [배터리 전류 A] [상전류 A]
상태
[1] 배터리 잔량 %   [2] 총 사용 전력량 Wh   [3] (예비)   [4] (예비)
<기록 시작/중지>  <시각화>  <프로그램 종료>
```

- **[1] 배터리 잔량** : `(V − BATT_V_EMPTY) / (BATT_V_FULL − BATT_V_EMPTY)`. ★부하 중엔 내부저항 전압강하 때문에 실제보다 낮게 나오는 **근사치**이지 정밀 SOC가 아니다★ 표시는 2초 이동평균(전압 흔들림 억제), CSV에는 원시 전압을 넣는다
  - **실제 팩** : 52V 80Ah, `14INR21/70`, **14S16P**, Samsung SDI INR21700-50S (2026-08-06 확인)

    | 기준 | 전압 | 근거 |
    |---|---|---|
    | 만충 (`BATT_V_FULL`) | **58.8 V** | 14S × 4.20V |
    | 공칭 | 51.8 V | 14S × 3.70V = 표기 52V |
    | 실용 하한 (`BATT_V_EMPTY`) | **42.0 V** | 14S × 3.00V. 이 아래로는 전압이 급락해 선형환산이 무의미하고 BMS도 보통 여기서 컷 |
    | 절대 하한 | 35.0 V | 14S × 2.50V (셀 데이터시트 방전종지) |

    용량도 16P × 5.0Ah = 80Ah로 표기와 맞는다.
  - ★ **실측 만충이 약 58V이므로 이 환산에서 만충은 약 95%로 표시된다**(실측: 58.0V → 95.2%, 52.0V → 59.5%). 충전기가 4.2V/셀까지 올리지 않는다는 뜻이고 틀린 표시가 아니다. 만충을 100%로 보이게 하려면 `BATT_V_FULL`을 58.0으로 내리면 된다 ★
  - ★ **`BATT_V_FULL`/`EMPTY` 상수는 [1]에만 쓰인다** ★ 반면 **전압 측정값 자체**는 [2] 전력량(`V × 버스전류`)과 '실시간 소모전력' 그래프에도 쓰인다. 즉 이 두 상수를 잘못 잡아도 [2]는 영향이 없다
- **[2] 총 사용 전력량** : `배터리전압 × 버스전류`를 시간 적분(Wh). 기록 중 실시간 누적, 중지 시 최종값. **버스전류(배터리 DC)를 쓰는 이유** — 상전류는 인버터 건너편 3상 AC라 상전압을 모르면 전력을 계산할 수 없고, 인버터 변환손실 때문에 배터리 실소모와도 어긋난다. 상전류는 모터 부하/발열 진단용 참고값
  - 회생(음수 전류)은 방전/회생을 나눠 누적하고, 표시값은 순 소비량(회생 차감분)이다. 내역은 `summary.txt`에 남는다
- **[3] [4]** : 요청대로 칸만 만들어 비워둠
- **기록** : ★폴더를 만들지 않고 **평평한 파일**로 쌓는다★ — `canlog_001.csv`, `canlog_002.csv` … + 종료 시 옆에 `canlog_001_summary.txt`. 번호는 001→…→999→**000**→001(덮어쓰기) 순환이며, 번호가 순환하므로 다음 번호는 '가장 최근에 쓴 파일'의 mtime 기준으로 정한다. 기록 시작 시 이전에 불러와 띄운 값은 화면에서만 지워지고 **디스크 데이터는 보존**된다
  - 후보 탐색은 `canlog_NNN.csv` 정확히 일치하는 **파일**만 센다 — 같은 이름의 폴더, `canlog_abc.csv`, `_summary.txt`, 아두이노 SD 파일명(`CANLOG01.CSV`), 구버전 `csvdata_NNN/` 폴더는 모두 무시한다(실측 검증)
  - CSV는 매 줄 `flush()` 한다(주행 중 전원이 끊겨도 마지막 줄까지 남기려고 — SD 로거와 같은 이유). 그래서 잘린 마지막 줄이 생길 수 있고, 읽는 쪽은 그 줄을 건너뛴다
- **시각화** : 이번 세션 기록 > 시작할 때 불러온 CSV 순으로 골라 **창 4개**를 띄운다 — `시간별 배터리 퍼센트` / `실시간 버스전류와 상전류`(2계열) / `실시간 소모전력`(V×버스전류) / `시간별 RPM`. 기록 중에도 볼 수 있다
  - **파일을 먼저 읽고 나서 파생값을 계산**하는 순서다(CSV에는 원시 열만 있으므로). 시간축은 첫 줄을 0으로 맞춘다 — 아두이노 `millis()`는 부팅 후 경과라 1048처럼 시작하기 때문
  - 양식이 아닌 파일을 고르면 없는 열 이름까지 알려주며 거부한다
  - pyplot을 쓰면 tkinter mainloop와 이벤트 루프가 얽히므로 `Toplevel` + `FigureCanvasTkAgg`로 직접 붙인다. 한글 폰트(`Malgun Gothic`)와 `axes.unicode_minus=False`를 지정하지 않으면 제목·축이 두부(□)로 깨진다
- **포트** : Windows COM 포트 중 아두이노로 보이는 것을 점수순으로 골라 **묻지 않고** 연결한다(메가 VID/PID > "mega" > "arduino" > CH340/FTDI/CP210x > USB-Serial). ★블루투스 가상 COM은 제외★ — 실측상 이 PC엔 COM3/COM4가 블루투스로 잡혀 있어 거르지 않으면 오연결한다
- **끊김 대응** : 아두이노 없이 실행해도 GUI는 정상 동작하며, 못 찾거나 도중 끊기면 **3초 간격 재연결**을 무한 반복한다. USB를 뽑아도 예외 없이 타임아웃만 반복되는 경우가 있어 `DATA_WATCHDOG_S`(5초) 무응답 워치독을 따로 둔다
- 종료 시 기록 중이었다면 **자동으로 기록을 마감**(요약 파일까지)하고 닫는다

### 주의

- A/B보드(`kasa_0804_*.ino`)와 시리얼 포트·프로토콜을 공유하지 않는 **별도 USB 연결**이 필요하다(포트 3개: A, B, CAN 브리지)
- 전비(Wh/km)는 아직 못 낸다 — **거리 정보가 없기 때문**. rpm→속도→거리로 환산하려면 타이어 둘레와 기어비(직결이면 1:1)가 필요하고, 이 CAN이 보는 모터가 1절의 인휠(175/60R13, 직결)과 같은 차량인지도 확정되지 않았다. 확정되면 [3]에 누적 주행거리, [4]에 전비를 넣을 자리는 이미 비워 뒀다
- 저장소에 남아 있는 `CANLOG.CSV`는 SD 로거의 실제 출력물이며, **현행 16열 양식과 동일**하다. 그래서 `y_can.py`로 바로 불러 시각화가 된다(테스트에 이 파일을 그대로 썼다). git 미추적 로컬 산출물
- 기록 산출물(`canlog_*.csv`, `canlog_*_summary.txt`, `CANLOG*.CSV`)은 `.gitignore`에 있다. 파일이 **스크립트가 있는 `code/` 안에** 쌓이기 때문
