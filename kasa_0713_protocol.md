# kasa 0713 2-보드 분리 입출력 규약

kasa_0712_none.ino(단일 Mega 2560)를 두 보드로 분리한 구성의 시리얼 통신 규약.

> ⚠️ **이 문서는 0713 시점의 스냅샷이다.** 아래 핀 배정과 입출력 형식은 이후 여러 차례 바뀌었다.
> **현재 운용 중인 규약은 `CLAUDE.md` 11절(0730 버전)을 볼 것.** 주요 차이:
> - 홀센서 핀 : 20/21 → **2(왼쪽)/21(오른쪽)**, 인휠 PWM : 10 공유 → **8(왼쪽)/9(오른쪽) 분리**
> - A입력 : 펄스 1개 → `<좌>,<우>` 콤마 2값 지원(0~15 펄스 / 16~255 직접 PWM / 256↑ 정지)
> - A출력 : `S,<좌>,<우>` → `S,<좌>,<우>,<쓰로틀raw>` (필드 4개)
> - B출력 : `P,<각도>` → `P,<각도>,<모드>` (D5 주행모드 스위치 추가), B입력 조향각에 `x`(힘빼기) 허용

- **A보드** (`kasa_0713_A.ino`) : 인휠모터 PID + 주행 펄스 계측 — 핀 10(인휠 PWM), 20/21(홀센서), 13(E-stop)
- **B보드** (`kasa_0713_B.ino`) : 조향 + 제동 — 핀 6/7(DC 조향), 8/9(리니어 브레이크), 13(E-stop)
- PC(ROS2 walker_k 노드)와 **각각 별도의 USB/시리얼 포트**로 연결 (115200 baud, 개행 `\n` 종료)
- 출력 접두어(`S,` / `P,`)로 포트 순서와 무관하게 출처 식별 가능

---

## A보드

### 입력 (PC → A)
```
<주행목표펄스>\n
```
- 정수 1개만 (예: `12`)
- 음수는 0으로 클램프
- 형식이 안 맞는 줄(토큰 2개 이상, 숫자 아님)은 무시
- ~~mode 필드~~ 폐기됨 (0712의 4번째 소프트웨어 e-stop 토큰 없음)

### 출력 (A → PC, 50ms 주기)
```
S,<21번펄스>,<20번펄스>\n
```
- 예: `S,3,5` → 21번(PID 피드백용)=3, 20번(모니터링용)=5
- **21번이 앞, 20번이 뒤** (0712의 "20 21" 순서와 다름에 주의)

### E-stop 중 출력
```
STOP\n
```

---

## B보드

### 입력 (PC → B)
```
<조향각도>,<브레이크출력>\n
```
- 콤마 구분 정수 2개 (예: `-10,-50` → 조향각 -10, 브레이크 -50)
- 조향각: -40~40 클램프, 음수=왼쪽 / 양수=오른쪽 / 0=정지 (열린루프, PWM = |각도|×4)
- 브레이크: -255~255 클램프, 부호=방향, 크기=PWM, 수신 시점부터 2초 타이밍 구동
- 형식이 안 맞는 줄은 무시
- ~~mode 필드~~ 폐기됨

### 출력 (B → PC, 50ms 주기)
```
P,<조향각환산값>\n
```
- 조향각환산값 = 가변저항 환산 현재 조향각
- **현재는 가변저항 미장착 → 항상 `P,0` 고정 출력** (추후 ADC 장착 시 교체)

### E-stop 중 출력
```
STOP\n
```

---

## E-stop (양 보드 공통, 독립 판정)

### 하드웨어
- 스위치: AK-C211008, NC 접점 2개 **직렬** 사용
  - `아두이노GND ― COM2 / NC2 ― COM1 / NC1 ― 13번(A·B 양쪽 병렬)`
- 각 보드 13번 핀 INPUT_PULLUP, 평상시 LOW(단락), 개방 시 HIGH
- **A·B 보드 GND 공통 필수** (별도 전원이면 GND끼리 연결)
- 두 보드는 서로 통신하지 않고 각자 독립적으로 감지·정지·`STOP` 출력

### 발동 조건 (각 보드에서 매 루프 재평가)
- 13번 핀 500ms 이상 연속 HIGH (디바운스) — **오직 이 외부 개입(하드웨어 핀)만이 조건**. 무입력 타임아웃 e-stop은 제거됨(2026-07-13)

### 발동 시 동작
- **A보드**: 인휠 PWM 0, PID 상태(적분/펄스 누적) 리셋, `STOP` 출력
- **B보드**: 조향 PWM 0, 리니어 최고출력(255) 체결 방향 2초 구동 후 정지, `STOP` 출력
- 조건 해소 시 자동 해제(래치 없음), B보드는 해제 엣지에서 리니어 강제 정지

### ROS2 측 처리 (walker_k 노드)
- 두 포트 중 **한쪽에서라도** `STOP` 수신 시 전체 정지 처리 (OR)
